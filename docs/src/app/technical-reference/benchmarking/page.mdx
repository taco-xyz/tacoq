```javascript
var s = "JavaScript syntax highlighting";
alert(s);
```

```yaml
name: My Project
version: 1.0.0
description: A simple project
dependencies:
  - python=3.10
  - pandas=1.5.3
  - numpy=1.24.3
```

```bash
pip install -r requirements.txt
```

```js {1,3-4}
function fancyAlert(arg) {
  if (arg) {
    $.facebox({ div: "#foo" });
  }
}
```

```diff-js showLineNumbers
+ function fancyAlert(arg1) {
- function fancyAlert(arg0) {
    if (arg) {
        $.facebox({ div: '#foo' })
    }
    return "Hello, world!";
}
```

Inline code: `console.log('Hello, world!');`
